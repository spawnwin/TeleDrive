# Токен от @BotFather
BOT_TOKEN=123456:ABC-DEF

# Публичный HTTPS URL Mini App (для кнопки в боте)
# Локально: используйте ngrok / cloudflared
WEBAPP_URL=https://your-domain.example

# Порт сервера (бот + API + статика)
PORT=3000

# Секрет для проверки initData (тот же BOT_TOKEN используется по умолчанию)
# BOT_TOKEN достаточно для HMAC-проверки Telegram WebApp
